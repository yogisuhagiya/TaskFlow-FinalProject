// In TaskFlow.Library/Services/ITaskService.cs
namespace TaskFlow.Library.Services
{
    public interface TaskService
    {
        // Just having the file is enough for now.
        // Later, you'd add method definitions like:
        // string GetSomeTaskInfo(int taskId);
    }
}